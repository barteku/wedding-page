{% extends "::base.html.twig" %}


{% block body %}
    {% block content_header %}
        <div class="navbar navbar-inverse navbar-fixed-bottom">
            <div class="navbar-inner">
                <div class="container">
                    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </a>
                    <a class="brand" href="{{ path('homepage') }}">Joasia & Bartek</a>
                    <div class="nav-collapse collapse">
                        <ul class="nav">
                            <li class="active"><a href="{{ path('homepage') }}">Home</a></li>
                            <li><a href="{{ path('place') }}">Miejsce</a></li>
                            <li><a href="#contact">Kontakt</a></li>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Szczegóły <b class="caret"></b></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ path('place') }}">Miejsce</a></li>
                                    <li><a href="#">Odzienie</a></li>
                                    <li><a href="#">Harmonogram</a></li>
                                    <li><a href="{{ path('registration') }}">Rejestracja</a></li>
                                </ul>
                            </li>
                        </ul>
                        
                    </div><!--/.nav-collapse -->
                </div>
            </div>
        </div>
    {% endblock %}
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="container">
            <div class="flash-message">
                <em>Notice</em>: {{ flashMessage }}
            </div>
        </div>
    {% endfor %}
    <div class="container">
        {% block content %}{% endblock %}
    </div>

{% endblock %}
